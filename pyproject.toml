[project]
name = "fbd"
version = "0.1.0"
description = "Forecast Bust Detection for medium-range weather forecasts over India (SIH26079)"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
